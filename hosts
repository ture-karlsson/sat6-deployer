# In order to run the standalone playbooks we need to do an entry of what the 
# add_host module would do to the temporary inventory
# The standalone playbooks looks for the [satellite] group
# Copy the example and enter the instance ip or fqdn 

[satellite]
# Example
# 10.250.1.1 ansible_user=ec2-user ansible_become=True ansible_ssh_private_key_file="files/satellite-cc-keypair.pem" ansible_ssh_common_args="-o StrictHostKeyChecking=no"


